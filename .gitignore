# claude-vault-drive/.gitignore
# Config locale à la machine (règles de dev, permissions, chemins sensibles) —
# jamais versionnée. Le plugin lui-même vit à la racine (commands/, scripts/…).
.claude/
/CLAUDE.md

# artefacts Python
__pycache__/
*.pyc
